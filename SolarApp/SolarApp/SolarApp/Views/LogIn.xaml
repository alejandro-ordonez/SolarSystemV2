<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputLayout="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SolarApp.Views.LogIn"
             Visual="Material"
             BackgroundImage="{Binding BgImage}"
             Title="Iniciar Sesion">
    <ContentPage.Content>
        <RelativeLayout HorizontalOptions="Fill" VerticalOptions="Fill">
            <StackLayout RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height}" RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width}" Spacing="50" Padding="30">
                <Label Style="{DynamicResource Title}" Text="{DynamicResource LogTitle}" HorizontalOptions="CenterAndExpand" Margin="0,50"/>
                <Frame BorderColor="White" BackgroundColor="White" Opacity="0.78" CornerRadius="5" VerticalOptions="Center" HorizontalOptions="Fill">
                    <StackLayout Spacing="30">
                        <inputLayout:SfTextInputLayout Hint="{StaticResource PHUser}" ContainerType="Outlined" HelperText="Ingrese su usuario">
                            <Entry Style="{DynamicResource Inputs}" Text="{Binding User}"/>
                        </inputLayout:SfTextInputLayout>
                        <inputLayout:SfTextInputLayout Hint="{StaticResource PHPass}" EnablePasswordVisibilityToggle="True" ContainerType="Outlined" HelperText="Ingrese su contraseña">
                            <Entry Style="{DynamicResource Inputs}" Text="{Binding Pass}"/>
                        </inputLayout:SfTextInputLayout>
                        <Button x:Name="Log" CornerRadius="50" Text="LogIn" Command="{Binding LogInCommand}"/>
                    </StackLayout>
                </Frame>
                <ActivityIndicator IsRunning="True" Color="Green" BackgroundColor="White"/>
            </StackLayout>
        </RelativeLayout>
    </ContentPage.Content>
</ContentPage>